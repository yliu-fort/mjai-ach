"""Integration tests: full-pipeline smoke tests on real games (AGENTS.md §5).

Slower than unit tests; gated to the pre-push stage via the ``slow`` marker and
the pre-push hook in .pre-commit-config.yaml.
"""
