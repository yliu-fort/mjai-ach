# 四问核查报告与修复方案（2026-07-22）

核查范围：AGENTS.md 治理约束下的四个验收问题。核查性质：只读审计（未修改任何仓库源文件），
动态验证使用 `MJAI_CPU=1` / `--cpu`（D6 明文允许）。`docs/reproduce_report.md` 按约定作为
可信来源直接采信。

---

## 一、四问判定总览

| # | 问题 | 判定 | 一句话结论 |
|---|---|---|---|
| Q1 | 论文复现参数是否已设为默认配置 | **部分** | 复现入口（`reproduce_paper.py` / `--config *_ach_mlp_mirror.yaml`）逐项一致；裸默认路径（`train.py --game/--algo/--mode` 不带 `--config`）6 项偏离论文，代码注释自标 "NN-tuned"，系有意的矩阵默认 |
| Q2 | 是否调查了 league play 配置策略及其对收敛的效果 | **未调查** | 0 个 league 训练 run、0 份 mirror-vs-league 对比产物、0 段文字结论；league 仅以代码 + README 研究问题 + 未执行 notebook 脚手架存在 |
| Q3 | 训练后策略是否 CLI 一键可配置可玩 | **部分（含阻断 bug）** | 机制齐备，但加载链路 100% 失败：CLI 重建 MLP 用默认 `(128,128)`，不读 sidecar 的 `hidden_sizes=(128,)`，runs/ 下 2449/2449 个 checkpoint 无一可加载 |
| Q4 | CLI 是否功能齐全、交互友好、界面美观 | **骨架完整，友好度低（主观）** | 选游戏→座位→模式闭环通、19 个单测通过、盲输入合规；但 TTT 棋盘渲染错乱、EOF 死循环、零视觉美化、2449 行 policy 列表无分页 |

---

## 二、Q1 证据摘要

**一致项（复现路径，18 项全 ✓）**：游戏串、MLP(128,)+ReLU 共享躯干、SGD lr=1e-3 恒定
（`nn_updates.py:261-264` 强制拒绝非 SGD）、value_coef=1.0、entropy_coef=0.01、hedge η=1.0、
l_th=2.0、ratio_eps=0.5、单侧门控、batch=64 样本（`target_samples: 64`）、单 mini-batch 单次
合并更新、GAE λ=0.95 / γ=1.0、loss_centered_logits=True、无梯度裁剪、total_env_steps=1e7、
eval_every=1e5、seeds 0–7（`reproduce_paper.py:58`）。

**裸默认路径偏离项（6 项，`src/mjai/scripts/experiment.py`）**：

| 参数 | 论文值 | 裸默认值 | 出处 |
|---|---|---|---|
| policy_kind | mlp | `tabular`（ACH-tabular = CFR+ wrapper，非论文 ACH） | experiment.py:50 |
| learning_rate | 1e-3 | 1e-4（注释自标 NN-tuned） | experiment.py:67 |
| value_coef | 1.0 | 0.5 | experiment.py:79 |
| entropy_coef | 1e-2 | 0.05 | experiment.py:68 |
| max_grad_norm | 0 | 0.5 | experiment.py:81 |
| 批次/总长口径 | 64 样本 / 1e7 env steps | 整 50 episodes/轮 / 500 轮 round 模式 | experiment.py:52,64,88-89 |

**次要违规**：`configs/games/*.yaml` 未知键被静默忽略（`game_config.py:55`），违反 §9
"unknown keys error loudly"（exp YAML 侧经 `ExperimentConfig(**data)` 是严格的）。

## 三、Q2 证据摘要

**league 策略旋钮（默认值）**：`LeagueMix` 50/30/20（opponent_sampler.py:36-38）、桶内 PFSP
ε=0.05（:26,125）；capacity=16、main_save_every=200、promo 阈值 0.55/0.55、
league_exploiter_share=0.70、promo_window=20、reset_mode="to_main"（manager.py:32-44）；
controller episodes_per_round=50（league_controller.py:48）。

**关键事实**：
- 全部 14 对 `*_league.yaml` vs `*_mirror.yaml` 每对仅差 3 行（注释 / self_play_mode / out_dir），
  league 模式**未调过任何超参**。
- 旋钮中仅 `league_capacity` 接进 YAML（experiment.py:71,196-198）；mix 权重、promo 阈值、
  promo_window、reset_mode、exploiter_keep 全部不可配（§9 违规模式）。
- runs/ 下 42 个 TB event 文件全部属于 mirror run；`gap_probe` 三臂也全是 mirror。
- 无任何 `*_mlp_league.yaml`——league 从未接入论文保真的 MLP/env-step 协议。
- tools（summarize_reproduce.py:42、compare_with_paper.py:141）硬编码 `*_ach_mlp_mirror` glob。
- 附带：`CheckpointStore.exploiter_keep` 是死字段（checkpoint_store.py:61 定义后无人消费）；
  checkpoint 池无 weak-ref/TTL，docstring 明示以 size-bound 代替——与 §8 字面要求不一致，
  属于"有意偏离但未修订 AGENTS.md"。
- ✅ 分层约束通过：league 未 import 具体 algos；import-linter 三条契约齐备
  （pyproject.toml:173-209），pre-commit 已接线。

## 四、Q3/Q4 证据摘要

**Q3 阻断点**：`main.py:62-64` 重建 MLP 只传 `obs_size/num_actions/seed`，忽略
`MLP.save()` 专为重建写出的 `policy.pt.meta.json`（mlp.py:200-221）。实测加载
`runs/reproduce/kuhn_ach_mlp_mirror/seed_7/checkpoints/step_154433` → `RuntimeError:
Missing key(s): "torso.2.weight"...`。runs/ 下 2449 个 checkpoint 全为 `(128,)`，全军覆没。

**Q3 次要缺陷**：policy 选择器不按游戏过滤、不校验 `manifest.game`/obs_size 匹配、无分页
（实测一次打印 2449 行）；加载无 try/except，traceback 直接抛给用户。

**Q4 实测发现**：
- ✅ 机制：console script（pyproject.toml:52）、7 游戏 renderer+parser 齐备、19 个 CLI 单测通过、
  auto 对局跑通、坏输入逐条拒绝。
- ✅ 盲输入合规（§4 强制项）：brps / goofspiel5_ii / oshi_zumo 的 parser 提示与
  `match_runner.py:102-116 _simultaneous_step` 逐座位渲染机制均不泄露对手同时选择。
- ❌ TTT renderer 平面语义错误（renderers/ttt.py:20-29）：OpenSpiel plane0=empty，X 在 plane2，
  renderer 从不读取 → 空棋盘显示满盘 X，棋盘不可读。
- ❌ Oshi-Zumo wrestler 位置解析失败兜底显示 `cell -1`（renderers/oshi_zumo.py:56-60）。
- ❌ 菜单 stdin EOF 死循环刷屏（main.py:136-138）；对局中 EOF 抛 traceback（match_runner.py:129）。
- ❌ docstring 承诺的 replay/quit 循环未实现（main.py:3-5 vs :125-129）。
- ❌ `rich` 依赖已声明（pyproject.toml:29-30 注释承诺 panels/tables/color）但 cli 零引用：
  无颜色/清屏/分隔线，菜单 blurb 为 ~200 字符长文，kuhn 动作 1 标注 "(fold)" 误导。

---

## 五、修复方案（按优先级；每项独立成 PR，遵守 §10 一个逻辑变更一个 commit）

### P0 — 阻断

**F1. 修复 CLI checkpoint 加载（Q3 核心）**
- 动作：加载链路改为元数据驱动——读取 sidecar `policy.pt.meta.json`（hidden_sizes/activation）
  与 `manifest.json`（game/obs_size/num_actions）重建正确架构；重建逻辑下沉到
  `agents/ckpt_io.py`（或新 `agents/policy_factory.py`），CLI 只面向统一接口，避免 `isinstance`
  向下转型（§3.3）。加载失败包 try/except，给用户一行可读错误。
- 文件：`src/mjai/cli/main.py`、`src/mjai/agents/ckpt_io.py`（+ 可能新增小模块）、
  `src/mjai/cli/policy_registry.py`。
- 验证：新增单测（小 MLP ckpt 保存→CLI 加载往返）+ CLI smoke 用真实 runs/ checkpoint 跑一局
  auto；`uv run pre-commit run --all-files`。

**F2. 补齐 league 配置策略调查（Q2 核心，分 5 步，最大工作块）**
1. 配置接线：`LeagueMix` 权重、promo 阈值×2、league_exploiter_share、promo_window、
   reset_mode 接进 `ExperimentConfig`/YAML（未知键已有 TypeError 保障）；删除或启用死字段
   `exploiter_keep`。
2. 新增 `configs/exp/*_mlp_league.yaml`：至少 kuhn + 一个 cycling 游戏（建议 oshi_zumo），
   协议对齐复现配置（MLP、env-step 口径）。
3. tools 汇总管线支持 league glob（summarize_reproduce.py / compare_with_paper.py 参数化模式）。
4. 跑 A/B：kuhn + oshi_zumo × {mirror, league} × 4–8 seeds（GPU，D6）；产物进
   `runs/league_probe/`；产出 exploitability 收敛对比曲线。
5. 写 `docs/league_investigation.md`：配置策略对比（默认 50/30/20 vs 至少一组变体，如
   history 权重加大）+ 对收敛影响的结论，回填 README 研究问题。

### P1 — 高

**F3. TTT renderer 平面语义修复**：按 OpenSpiel 实际语义（plane0=empty、X=plane2）重写渲染；
单测断言空棋盘全空、指定落子显示正确符号。
**F4. Oshi-Zumo wrestler 位置解析修复**（oshi_zumo.py:56-60），解析失败时显式报错而非兜底 -1。
**F5. policy 选择器**：按所选游戏过滤 + 校验 game/obs_size 匹配 + 分页/截断（默认前 20 条 +
按步数排序 + 可输入前缀搜索）。
**F6. 裸默认路径 warn-loud（Q1 收尾）**：`train.py` 不带 `--config` 时打印醒目警告"当前为
NN-tuned 矩阵默认，非论文复现配置；复现请用 `configs/exp/*_ach_mlp_mirror.yaml` 或
`reproduce_paper.py`"。不改默认值本身（保护 2×2 矩阵实验），也不做静默切换（§11 禁静默
fallback）。

### P2 — 中

**F7. EOF 健壮性**：菜单 EOF 优雅退出（返回码 0 + 一行提示）；对局中 EOF 优雅终局，两处都加
回归测试（`printf '' | mjai-play` 必须干净退出）。
**F8. replay/quit 循环**：实现 docstring 已承诺的赛后 replay/quit 菜单（或删承诺；建议实现）。
**F9. `configs/games` 未知键报错**（game_config.py:55，对齐 §9）。
**F10. checkpoint 池与 §8 对齐**：二选一——给 CheckpointStore 加 TTL/weak-ref，或经评审修订
AGENTS.md §8 明确"size-bound 代替 weak-ref"为正式决策（D11）。

### P3 — 低（美观与文案）

**F11. rich 美化**：兑现 pyproject 注释承诺——菜单面板、policy 表格、棋盘上色、清屏与分隔线；
菜单 blurb 截断为单行摘要（details 进 help 子项）。注意 renderer 接口不变、单测同步更新。
**F12. kuhn 动作名修正**：去掉动作 1 的误导性 "(fold)" 标注。
**F13. 加载提示**：`require_cpu()` 时打印一行说明（非静默）。

### 执行顺序建议

F1 → F3/F4/F5（CLI 正确性一组）→ F6（警告，半小时）→ F2（league 接线+实验，多日）→
F7–F10 → F11–F13。每个 PR 附带对应单测（§5/§10），合并前
`uv run pre-commit run --all-files` 全绿。

---

## 六、验证记录（本次审计实际执行）

- `uv run pytest tests/unit/test_cli_match_runner.py -q` → 19 passed。
- `printf ... | MJAI_CPU=1 uv run mjai-play` 多轮：BRPS auto 跑通；TTT 人机跑通（暴露渲染
  bug）；Oshi-Zumo 人机跑通（盲提示合规）；kuhn 加载真实 ckpt 复现 RuntimeError；
  菜单 EOF 死循环与对局 EOF traceback 均实测复现。
- 配置解析验证：三个 `_ach_mlp_mirror.yaml` 经 `ExperimentConfig(**data)` 解析 18 项关键
  字段与论文值逐项相同；裸默认值输出确认 6 项偏离。
- 全 14 对 league/mirror YAML diff：每对仅 3 行差异。
- runs/ 全量扫描：42 个 TB event 文件全属 mirror；2449 个 checkpoint 全为 `(128,)`。
- import-linter 契约与 league import 抽查：分层合规。

---

## 七、修复进度（2026-07-22 当日）

**已完成：F1 / F3 / F5**（工作树未提交，待按 §10 分 commit）。

- F1：新增 `src/mjai/agents/policy_factory.py`（元数据驱动重建：sidecar 优先、run
  config.json 溯源补充、冲突即响亮报错）；`MLP.save()` sidecar 增记 activation；
  `cli/main.py` 与 `scripts/evaluate.py` 均切换到该工厂（evaluate.py 旧路径是同类
  bug，本次一并修复）。
- F5：`cli/policy_registry.py` 新增按 game 过滤 / obs-动作空间校验 / 子串过滤 / 分页
  （20 条/页）；main.py 选择器友好化，EOF 干净退出。
- F3：`cli/renderers/ttt.py` 按实证语义修复（plane0=empty、plane1=O、plane2=X）。
- 测试：新增 25 个单测（policy_factory 12、registry 7、ttt renderer 6），全套件
  266 passed。
- 门禁：`GIT_CONFIG_COUNT=0 uv run pre-commit run --all-files` 14 项全绿（含
  AST 行数、import-linter、mypy strict、pytest fast suite）。
- e2e：mjai-play 加载真实 kuhn checkpoint（step=154446）auto 对局 EXIT=0；TTT 人机
  渲染正确；picker 按游戏过滤 + `… and N more` 分页正常；损坏/不匹配 ckpt 均友好报错；
  `mjai-eval --run runs/reproduce/kuhn_ach_mlp_mirror/seed_5 --cpu` 完整跑通，
  写出 eval_results.json。

**待办**：F2（league 调查）、F4、F6–F13。
