"""Scripts: human-facing entry points (train, evaluate, profile).

These import the rest of the package; nothing imports them (AGENTS.md §2 — cli
is a leaf; scripts sit alongside it as another leaf set, entered via the
console-script entry points in pyproject.toml).
"""
