# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# --- Virtual envs / uv ---
.venv/
venv/
env/
ENV/
.python-version
# uv.lock IS committed (D7: reproducible env). Do not ignore it.

# --- Testing / coverage ---
.cache/
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
coverage.xml
*.cover
*.py,cover
.hypothesis/

# --- Type checkers ---
.mypy_cache/
.dmypy.json
dmypy.json
.pyre/
.pytype/

# --- Ruff ---
.ruff_cache/

# --- IDEs ---
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# --- Jupyter ---
.ipynb_checkpoints/
*/.ipynb_checkpoints/*
# Notebooks themselves ARE committed (one-click deliverable). Only ignore checkpoints.

# --- TensorBoard / run outputs (D9: TensorBoard; runs are local artifacts) ---
runs/
runs/**/
events.out.tfevents.*
*.tfevents.*
logs/
profiles/
*.prof
*.profraw

# --- Saved checkpoints / policy artifacts (large, regenerable) ---
checkpoints/
policies/
*.ckpt
*.pt
*.pth
# Manifest sidecars (small JSON) ARE committed under configs/ or runs-metadata/;
# binary weights are not.

# --- OS ---
Thumbs.db
desktop.ini

# --- Ray ---
ray_results/
/tmp/ray/

# --- Local wheel cache (large downloads staged for install) ---
.cache/

# --- ZCode CLI session data (not part of the project) ---
.zcode/

# --- import-linter / build caches ---
.import_linter_cache/
